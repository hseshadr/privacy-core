{
  "name": "@edgeproc/privacy-core-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runnable Vite demo that consumes @edgeproc/privacy-core's public API — redact, preview, send, then rehydrate locally.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@edgeproc/privacy-core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
