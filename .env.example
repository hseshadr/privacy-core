# Copy to examples/demo/.env to enable the real OpenRouter provider.
# With NO key set, the demo runs on NoLLMProvider (offline echo) — it still works.
VITE_OPENROUTER_API_KEY=
# Optional: override the model (default below is cheap + OpenAI-compatible).
VITE_OPENROUTER_MODEL=openai/gpt-4o-mini
